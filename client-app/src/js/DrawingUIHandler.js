
/*
var sliderValue;

if (window.addEventListener) {
    window.addEventListener('load', function () {
        var width_slider = document.getElementById('widthSlider');
        var width_input = document.getElementById('widthInput');

        width_slider.addEventListener('click', sliderUpdate, false);
        width_input.addEventListener('change', inputUpdate, false);

        function sliderUpdate() {
            alert(width_slider.val);
            width_input.value = 10;
        }

        function inputUpdate() {
            width_slider = width_input;
        }
    });
}

export default function updateSlider(value) {
    sliderValue = value;
}*/